{
  "collectionName": "comments",
  "info": {
    "singularName": "comment",
    "pluralName": "comments",
    "displayName": "Comment",
    "description": "Kommentar in einer Runde (Frage, Antwort, Reaktion oder Perspektive)"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {},
  "attributes": {
    "content": {
      "type": "text",
      "required": true
    },
    "type": {
      "type": "enumeration",
      "enum": ["question", "answer", "reaction", "perspective"],
      "default": "question"
    },
    "user": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "plugin::users-permissions.user",
      "required": true
    },
    "round": {
      "type": "relation",
      "relation": "manyToOne",
      "target": "api::round.round",
      "required": true
    }
  }
}
